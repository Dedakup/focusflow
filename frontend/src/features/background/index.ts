import { backgroundSlice, selectBackground, setBackgrounds } from './slices/backgroundSlice';
import type { Background, BackgroundState } from './types';

export {
    backgroundSlice,
    selectBackground,
    setBackgrounds,
    type Background,
    type BackgroundState
};
